[project]
name = "bamul-shared-lib"
version = "0.1.0"
description = "Shared Python utilities for BAMUL services — Pydantic models, auth helpers."
requires-python = ">=3.12,<3.13"
dependencies = [
    "pydantic>=2.7.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
